# Getting Started with D3 in React

- run `npm install`
- run `npm start`
- **Note:** Import D3 by adding `import * as d3 from d3` . You need to use import * (“import everything”) since D3 has no default exported module.